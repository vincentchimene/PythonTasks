"""
A python program that declares a variable counter and uses the pre-increment 
operator three times, printing the value of the counter after each increment.
"""
counter = 1
print("initial counter = ", counter)
counter += counter
print("initial counter = ", counter)
counter += counter
print("initial counter = ", counter)
counter += counter
print("initial counter = ", counter)

